---
phase: 01-foundation-core-ui
plan: 04
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - client/src/pages/DashboardPage.tsx
  - client/src/components/detail/DetailPanel.tsx
  - client/src/components/detail/SummaryView.tsx
  - client/src/components/detail/EntryList.tsx
  - client/src/components/detail/EntryCard.tsx
  - client/src/components/detail/ScreenshotLightbox.tsx
  - client/src/pages/AnalyticsPage.tsx
  - client/src/components/analytics/SeverityChart.tsx
  - client/src/components/analytics/VolumeChart.tsx
  - client/src/components/analytics/CategoryChart.tsx
  - client/src/components/analytics/TopImpactTable.tsx
  - client/src/App.tsx
autonomous: false
requirements:
  - DETL-01
  - DETL-02
  - DETL-03
  - DASH-02

must_haves:
  truths:
    - "Clicking a group card on the dashboard opens a slide-in Sheet panel from the right (~60% viewport width)"
    - "Detail panel shows AI summary on the left with labeled sections: Problem, Impact, Reproduction Steps, Improvement Suggestion"
    - "Detail panel shows all raw feedback entries on the right as a scrollable card stack"
    - "Screenshots appear as inline thumbnails next to entries, clicking opens a lightbox/modal"
    - "Clicking the dimmed backdrop or pressing Escape closes the detail panel"
    - "Analytics page shows expanded charts: severity donut, volume area chart, category bar chart, top impacted areas table"
    - "Dashboard remains partially visible and dimmed behind the open detail panel"
  artifacts:
    - path: "client/src/components/detail/DetailPanel.tsx"
      provides: "Split-view detail panel (summary left, entries right)"
      min_lines: 40
    - path: "client/src/components/detail/SummaryView.tsx"
      provides: "AI summary display with Problem, Impact, Reproduction Steps, Suggestion sections"
      min_lines: 30
    - path: "client/src/components/detail/EntryList.tsx"
      provides: "Scrollable list of raw feedback entries"
      min_lines: 20
    - path: "client/src/components/detail/EntryCard.tsx"
      provides: "Individual feedback entry card with text, source, sentiment, screenshot thumbnail"
      min_lines: 30
    - path: "client/src/components/detail/ScreenshotLightbox.tsx"
      provides: "Image lightbox using shadcn/ui Dialog"
      min_lines: 20
    - path: "client/src/pages/AnalyticsPage.tsx"
      provides: "Full analytics page with expanded charts"
      min_lines: 40
    - path: "client/src/components/analytics/SeverityChart.tsx"
      provides: "Donut chart for severity distribution"
      min_lines: 30
    - path: "client/src/components/analytics/VolumeChart.tsx"
      provides: "Area chart for feedback volume over time"
      min_lines: 30
    - path: "client/src/components/analytics/CategoryChart.tsx"
      provides: "Bar chart for category breakdown"
      min_lines: 30
  key_links:
    - from: "client/src/pages/DashboardPage.tsx"
      to: "client/src/components/detail/DetailPanel.tsx"
      via: "Sheet component wrapping DetailPanel"
      pattern: "Sheet.*SheetContent.*DetailPanel"
    - from: "client/src/components/detail/DetailPanel.tsx"
      to: "client/src/lib/api.ts"
      via: "fetchGroup to load entries"
      pattern: "fetchGroup"
    - from: "client/src/components/detail/EntryCard.tsx"
      to: "client/src/components/detail/ScreenshotLightbox.tsx"
      via: "onScreenshotClick callback"
      pattern: "onScreenshotClick"
    - from: "client/src/components/detail/ScreenshotLightbox.tsx"
      to: "shadcn/ui Dialog"
      via: "Dialog component for image expansion"
      pattern: "Dialog.*DialogContent"
    - from: "client/src/pages/AnalyticsPage.tsx"
      to: "client/src/lib/api.ts"
      via: "fetchAnalytics on mount"
      pattern: "fetchAnalytics"
---

<objective>
Build the slide-in detail panel with AI summary and raw entries, the screenshot lightbox, and the full analytics page with expanded charts. This completes all Phase 1 UI requirements.

Purpose: The detail panel is the "depth" moment in the demo -- judges click a card and see structured AI analysis alongside raw feedback. The analytics page shows data-driven insights. Together they prove the product delivers real value.

Output: Complete, polished Phase 1 UI -- dashboard with detail panel, analytics page with charts, screenshots with lightbox. Demo-ready.
</objective>

<execution_context>
@/Users/mvrm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mvrm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-core-ui/01-RESEARCH.md
@.planning/phases/01-foundation-core-ui/01-03-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plans 01, 02, and 03 -->

From client/src/lib/types.ts:
```typescript
export interface FeedbackEntry {
  id: string
  text: string
  source: string
  submittedAt: string
  userName: string
  screenshotUrl?: string
  sessionId?: string
  sentiment?: "positive" | "negative" | "neutral"
  groupId: string
}

export interface FeedbackGroup {
  id: string
  title: string
  summary: string
  severity: "critical" | "high" | "medium" | "low"
  entryCount: number
  categories: string[]
  aiSummary: {
    problem: string
    impact: string
    reproductionSteps: string[]
    suggestion: string
  }
  duplicateCount: number
  trendDirection: "up" | "down" | "stable"
  firstReported: string
  lastReported: string
}

export interface AnalyticsData {
  totalEntries: number
  totalGroups: number
  severityDistribution: { severity: string; count: number }[]
  categoryBreakdown: { category: string; count: number }[]
  volumeByDate: { date: string; count: number }[]
  topImpactedAreas: { area: string; groupCount: number }[]
}
```

From client/src/lib/api.ts:
```typescript
export function fetchGroups(): Promise<FeedbackGroup[]>
export function fetchGroup(id: string): Promise<FeedbackGroup & { entries: FeedbackEntry[] }>
export function fetchAnalytics(): Promise<AnalyticsData>
```

From client/src/lib/severity.ts:
```typescript
export const severityConfig: Record<Severity, { color: string; dot: string; chart: string }>
export type Severity = "critical" | "high" | "medium" | "low"
```

From DashboardPage.tsx (Plan 03 creates this):
```typescript
// DashboardPage manages selectedGroup state
// Plan 04 adds the Sheet + DetailPanel around this state
const [selectedGroup, setSelectedGroup] = useState<FeedbackGroup | null>(null)
```

shadcn/ui components available (installed in Plan 02):
- Sheet, SheetContent, SheetHeader, SheetTitle (for slide-in panel)
- Dialog, DialogContent, DialogTrigger (for screenshot lightbox)
- Card, CardHeader, CardContent, CardTitle, CardDescription
- Badge
- ScrollArea
- Separator
- Skeleton
- ChartContainer, ChartTooltip, ChartTooltipContent (for charts)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build detail panel with AI summary, entry list, and screenshot lightbox</name>
  <files>
    client/src/pages/DashboardPage.tsx,
    client/src/components/detail/DetailPanel.tsx,
    client/src/components/detail/SummaryView.tsx,
    client/src/components/detail/EntryList.tsx,
    client/src/components/detail/EntryCard.tsx,
    client/src/components/detail/ScreenshotLightbox.tsx
  </files>
  <action>
    **Step 1: Create SummaryView.tsx (client/src/components/detail/SummaryView.tsx).**
    Props: `{ aiSummary: FeedbackGroup['aiSummary']; severity: Severity }`
    Per user's locked decision: AI summary structured with labeled sections.
    - 4 sections with clear labels and lucide-react icons:
      1. "Problem" (AlertCircle icon) -- aiSummary.problem text
      2. "Impact" (Users icon) -- aiSummary.impact text
      3. "Reproduction Steps" (ListOrdered icon) -- numbered list from aiSummary.reproductionSteps array
      4. "Improvement Suggestion" (Lightbulb icon) -- aiSummary.suggestion text
    - Each section: label in muted text, content in regular text
    - Severity badge at the top with full color indicator
    - Glass card sections with subtle separators between them
    - Use Separator from shadcn/ui between sections

    **Step 2: Create EntryCard.tsx (client/src/components/detail/EntryCard.tsx).**
    Props: `{ entry: FeedbackEntry; onScreenshotClick: (url: string) => void }`
    - Card showing: entry.text, entry.userName, entry.source (as badge), entry.submittedAt (formatted relative date like "3 days ago")
    - Sentiment indicator: small colored dot (green/red/gray for positive/negative/neutral)
    - If entry.screenshotUrl exists: show inline thumbnail (48x36px, rounded, border) that triggers onScreenshotClick when clicked. Thumbnail shows the actual image from /screenshots/ path.
    - If entry.sessionId exists: show a small "Session recorded" badge with Video icon (this is just a visual indicator; replay is Phase 3)
    - Glass card style matching dashboard cards: bg-card/50 backdrop-blur-sm border-white/10

    **Step 3: Create EntryList.tsx (client/src/components/detail/EntryList.tsx).**
    Props: `{ entries: FeedbackEntry[]; onScreenshotClick: (url: string) => void }`
    - ScrollArea wrapping a vertical stack of EntryCard components
    - Show entry count header: "{N} feedback entries"
    - Sort entries by submittedAt (most recent first)
    - Glass container style

    **Step 4: Create ScreenshotLightbox.tsx (client/src/components/detail/ScreenshotLightbox.tsx).**
    Props: `{ imageUrl: string | null; onClose: () => void }`
    - Use shadcn/ui Dialog component
    - Dialog opens when imageUrl is non-null, closes when onClose is called
    - DialogContent shows the full-size image (max-w-3xl, max-h-[80vh], object-contain)
    - Dark overlay backdrop (the default Dialog backdrop works)
    - Close on backdrop click or Escape key (Dialog handles this)

    **Step 5: Create DetailPanel.tsx (client/src/components/detail/DetailPanel.tsx).**
    Props: `{ group: FeedbackGroup }`
    Per user's locked decision: split view, AI summary on left, raw entries on right.
    - On mount: fetch full group data with entries via fetchGroup(group.id) using useEffect
    - State: entries (FeedbackEntry[]), loading (boolean), lightboxImage (string | null)
    - Layout: flex row, left side ~45% width (SummaryView), right side ~55% width (EntryList)
    - Left side: SummaryView with group.aiSummary and group.severity
    - Right side: EntryList with fetched entries, onScreenshotClick sets lightboxImage
    - ScreenshotLightbox at bottom, controlled by lightboxImage state
    - Show Skeleton loading state for entries while fetching
    - Title at top: group.title with severity badge and entry count

    **Step 6: Wire Sheet into DashboardPage.tsx.**
    Modify the existing DashboardPage to add the slide-in panel:
    - Import Sheet, SheetContent, SheetHeader, SheetTitle from shadcn/ui
    - Import DetailPanel
    - Add Sheet component wrapping SheetContent after the CardGrid
    - Sheet controlled by selectedGroup state: `open={!!selectedGroup}` `onOpenChange={() => setSelectedGroup(null)}`
    - SheetContent: `side="right"` `className="w-[60vw] sm:max-w-none p-0 border-l border-white/10"` per user decision (~60% viewport)
    - Inside SheetContent: render DetailPanel when selectedGroup is set
    - Per user decision: clicking dimmed backdrop or Escape closes panel (Sheet handles this by default)
    - Dashboard remains partially visible and dimmed behind (Sheet's built-in overlay)
  </action>
  <verify>
    <automated>cd /Users/mvrm/Desktop/Hackathons/c0mpiled-Tokyo2026/client && npx tsc --noEmit && node -e "const fs=require('fs'); const dp=fs.readFileSync('src/components/detail/DetailPanel.tsx','utf8'); const sv=fs.readFileSync('src/components/detail/SummaryView.tsx','utf8'); const el=fs.readFileSync('src/components/detail/EntryList.tsx','utf8'); const ec=fs.readFileSync('src/components/detail/EntryCard.tsx','utf8'); const sl=fs.readFileSync('src/components/detail/ScreenshotLightbox.tsx','utf8'); const dash=fs.readFileSync('src/pages/DashboardPage.tsx','utf8'); console.log('DETL-01 summary:', sv.includes('Problem') && sv.includes('Impact') && sv.includes('Suggestion') ? 'OK' : 'MISSING'); console.log('DETL-02 entries:', el.includes('EntryCard') && ec.includes('entry') ? 'OK' : 'MISSING'); console.log('DETL-03 screenshots:', ec.includes('screenshotUrl') && sl.includes('Dialog') ? 'OK' : 'MISSING'); console.log('Sheet wiring:', dash.includes('Sheet') && dash.includes('DetailPanel') ? 'OK' : 'MISSING'); const ok = dp.includes('fetchGroup') && sv.includes('Problem') && ec.includes('screenshotUrl') && dash.includes('Sheet'); process.exit(ok ? 0 : 1)"</automated>
  </verify>
  <done>
    - Clicking a group card opens the Sheet panel from the right at ~60% viewport width
    - Dashboard is dimmed behind the panel
    - Detail panel shows: group title, severity badge, entry count
    - Left side: AI summary with Problem, Impact, Reproduction Steps, Suggestion sections
    - Right side: Scrollable list of all group entries as cards
    - Entries show text, user, source, date, sentiment indicator
    - Entries with screenshots show inline thumbnail
    - Clicking thumbnail opens lightbox with full-size image
    - Entries with sessionId show "Session recorded" badge
    - Escape or backdrop click closes the panel
    - No TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Build analytics page with expanded charts</name>
  <files>
    client/src/pages/AnalyticsPage.tsx,
    client/src/components/analytics/SeverityChart.tsx,
    client/src/components/analytics/VolumeChart.tsx,
    client/src/components/analytics/CategoryChart.tsx,
    client/src/components/analytics/TopImpactTable.tsx,
    client/src/App.tsx
  </files>
  <action>
    **Step 1: Create SeverityChart.tsx (client/src/components/analytics/SeverityChart.tsx).**
    Props: `{ data: { severity: string; count: number }[] }`
    - Donut chart (PieChart with innerRadius) showing severity distribution
    - Use shadcn/ui ChartContainer with ChartConfig mapping severity to OKLCH colors from severityConfig
    - `className="min-h-[300px] w-full"` on ChartContainer (pitfall: charts need min-h)
    - Center label showing total count
    - Legend below the chart
    - ChartTooltip with ChartTooltipContent for hover details
    - Glass card wrapper: Card with bg-card/50 backdrop-blur-md border-white/10
    - Title: "Severity Distribution"

    **Step 2: Create VolumeChart.tsx (client/src/components/analytics/VolumeChart.tsx).**
    Props: `{ data: { date: string; count: number }[] }`
    - Area chart (AreaChart from Recharts) showing feedback volume over past 30 days
    - Gradient fill under the line using linearGradient with accent color
    - ChartContainer with min-h-[300px]
    - X-axis showing dates (formatted as "Mar 1", "Mar 5" etc.), Y-axis showing count
    - ChartTooltip for hover details
    - Glass card wrapper with title "Feedback Volume (30 days)"

    **Step 3: Create CategoryChart.tsx (client/src/components/analytics/CategoryChart.tsx).**
    Props: `{ data: { category: string; count: number }[] }`
    - Horizontal bar chart (BarChart from Recharts with layout="vertical")
    - Bars colored with a vibrant accent color (one color, varying opacity or a gradient)
    - ChartContainer with min-h-[300px]
    - Y-axis showing category names, X-axis showing counts
    - Glass card wrapper with title "Category Breakdown"

    **Step 4: Create TopImpactTable.tsx (client/src/components/analytics/TopImpactTable.tsx).**
    Props: `{ data: { area: string; groupCount: number }[] }`
    - Table/list showing top impacted areas ranked by group count
    - Each row: rank number, area name, group count as badge, visual bar proportional to count
    - Glass card wrapper with title "Most Impacted Areas"
    - Use Separator between rows
    - Keep it simple and visually clean -- this is PM-useful data (DASH-02 extended view)

    **Step 5: Create AnalyticsPage.tsx (client/src/pages/AnalyticsPage.tsx).**
    - On mount: fetch analytics data via fetchAnalytics() using useEffect
    - State: analytics (AnalyticsData | null), loading (boolean)
    - Layout: 2-column grid at the top (SeverityChart left, VolumeChart right)
    - Below: 2-column grid (CategoryChart left, TopImpactTable right)
    - Show summary stats row at very top: total entries, total groups, critical count (similar to dashboard but with more detail text)
    - Skeleton loading state while fetching
    - Glass aesthetic matching the dashboard

    **Step 6: Update App.tsx.**
    - Import AnalyticsPage
    - Replace the /analytics route placeholder with the real AnalyticsPage component
  </action>
  <verify>
    <automated>cd /Users/mvrm/Desktop/Hackathons/c0mpiled-Tokyo2026/client && npx tsc --noEmit && node -e "const fs=require('fs'); const ap=fs.readFileSync('src/pages/AnalyticsPage.tsx','utf8'); const sc=fs.readFileSync('src/components/analytics/SeverityChart.tsx','utf8'); const vc=fs.readFileSync('src/components/analytics/VolumeChart.tsx','utf8'); const cc=fs.readFileSync('src/components/analytics/CategoryChart.tsx','utf8'); console.log('Analytics fetches data:', ap.includes('fetchAnalytics') ? 'OK' : 'MISSING'); console.log('Severity chart:', sc.includes('PieChart') ? 'OK' : 'MISSING'); console.log('Volume chart:', vc.includes('AreaChart') ? 'OK' : 'MISSING'); console.log('Category chart:', cc.includes('BarChart') ? 'OK' : 'MISSING'); console.log('Min height set:', sc.includes('min-h') && vc.includes('min-h') ? 'OK' : 'MISSING'); const ok = ap.includes('fetchAnalytics') && sc.includes('PieChart') && vc.includes('AreaChart') && cc.includes('BarChart'); process.exit(ok ? 0 : 1)"</automated>
  </verify>
  <done>
    - Navigating to /analytics shows the full analytics page
    - Severity donut chart renders with correct colors per severity level
    - Volume area chart shows 30-day trend line with gradient fill
    - Category bar chart shows horizontal bars for each category
    - Top impacted areas table shows ranked list with visual bars
    - All charts render without zero-height issues (min-h applied)
    - Glass card wrappers on all chart sections
    - Data loads from API on page mount
    - No TypeScript errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete Phase 1 UI</name>
  <files>none</files>
  <action>
    Human verifies the complete Phase 1 UI is demo-ready. All automated work was completed in Tasks 1 and 2.

    What was built: Complete Phase 1 UI including dashboard with glass-card grid, stats row, PM insights, slide-in detail panel with AI summary and entry list, screenshot lightbox, and full analytics page with charts.

    How to verify:
    1. Run `npm run dev` from the project root
    2. Open http://localhost:5173 in your browser
    3. Dashboard check: Verify stats row at top (4 metrics with mini-charts), PM insights badges below, and 2-column card grid with 8-12 feedback groups
    4. Card check: Each card should show title, summary, entry count, severity color, and category pills
    5. Glass aesthetic check: Cards should have semi-transparent glass look with backdrop blur on dark background -- Raycast/Arc inspired
    6. Detail panel: Click any group card -- a panel should slide in from the right covering ~60% of the viewport. Dashboard should be dimmed behind it.
    7. Detail content: Panel should show AI summary (Problem, Impact, Reproduction Steps, Suggestion) on the left, raw entries on the right
    8. Screenshots: Some entries should show small thumbnail images. Click a thumbnail to open the lightbox modal.
    9. Close panel: Press Escape or click the dimmed area to close
    10. Analytics page: Click the "Analytics" tab in the header. Verify severity donut chart, volume area chart, category bar chart, and top impacted areas are all visible and rendering data.
    11. Navigation: Switch between Dashboard and Analytics tabs -- smooth transitions, correct active tab highlight
  </action>
  <verify>
    <automated>cd /Users/mvrm/Desktop/Hackathons/c0mpiled-Tokyo2026/client && npx tsc --noEmit && echo "TypeScript OK"</automated>
  </verify>
  <done>User confirms the Phase 1 UI is demo-ready by typing "approved", or provides specific issues to fix</done>
</task>

</tasks>

<verification>
1. `npm run dev` starts both servers
2. Dashboard: stats row, insights row, 8-12 glass cards in 2-column grid
3. Click card: Sheet panel slides in from right at ~60vw
4. Detail panel: AI summary (left) with 4 labeled sections, entries (right) as scrollable cards
5. Screenshots: thumbnails visible on some entries, lightbox opens on click
6. Analytics page: 4 charts render with data, glass card styling
7. Navigation between Dashboard and Analytics works via header tabs
8. TypeScript compiles clean: `cd client && npx tsc --noEmit`
</verification>

<success_criteria>
- The complete Phase 1 UI is demo-ready on a large screen
- Dashboard immediately communicates "chaos into clarity" with polished glass cards and severity indicators
- Detail view shows depth: structured AI analysis + raw feedback with screenshots
- Analytics provides data-driven insights with professional charts
- All navigation flows work: dashboard -> detail panel -> close, dashboard -> analytics -> dashboard
- Visual quality is "wow" level for hackathon judges
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-ui/01-04-SUMMARY.md`
</output>
